---
description: Run the installed Cyber Neo security audit skill
---

Use the installed `cyber-neo` skill from this repository to run a read-only security audit.

- If `$ARGUMENTS` is provided, treat it as the target path.
- If `$ARGUMENTS` is empty, audit the current repository root.
- Follow the skill's read-only rules.
- Generate the security report requested by the skill and summarize the main findings for the user.
